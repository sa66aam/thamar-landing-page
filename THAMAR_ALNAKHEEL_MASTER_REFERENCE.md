# مرجع ثمر النخيل الشامل | Thamar Alnakheel Master Reference
### الوثيقة المرجعية الرئيسية للمشروع — لا تحذف هذا الملف

> آخر تحديث: فبراير 2026
> المالك: Jo (sa66aam@gmail.com)

---

## الفهرس

1. [نظرة عامة على المشروع](#1-نظرة-عامة-على-المشروع)
2. [الهوية البصرية والعلامة التجارية](#2-الهوية-البصرية-والعلامة-التجارية)
3. [البنية التقنية](#3-البنية-التقنية)
4. [صفحات التطبيق والمكونات](#4-صفحات-التطبيق-والمكونات)
5. [نظام Firebase والتحليلات](#5-نظام-firebase-والتحليلات)
6. [المنتجات والتسعير](#6-المنتجات-والتسعير)
7. [تطبيق المحاسبة (المشروع الثاني)](#7-تطبيق-المحاسبة-المشروع-الثاني)
8. [تصميم الفلاير A4](#8-تصميم-الفلاير-a4)
9. [المشاكل والحلول (Lessons Learned)](#9-المشاكل-والحلول-lessons-learned)
10. [الروابط والمسارات المهمة](#10-الروابط-والمسارات-المهمة)
11. [أوامر التشغيل السريعة](#11-أوامر-التشغيل-السريعة)
12. [خريطة الملفات](#12-خريطة-الملفات)
13. [ملاحظات النشر والتحديث](#13-ملاحظات-النشر-والتحديث)

---

## 1. نظرة عامة على المشروع

ثمر النخيل هو مشروع أعمال منزلية سعودي متخصص في تمور فاخرة محشوة ومزيّنة. المشروع يتكون من مشروعين فرعيين:

**أ. موقع التسويق (Landing Page)** — المشروع الرئيسي النشط
- موقع تسويقي فاخر يعرض المنتجات، معرض الصور/الفيديو للمناسبات، ونظام تقييمات وتعليقات حيّ.
- الهدف: جذب العملاء عبر واتساب، عرض المنتجات بشكل احترافي، وعرض ضيافة المناسبات.

**ب. تطبيق المحاسبة (Accounting App)** — مشروع مساند
- مستشار مالي ذكي لحساب التكاليف والتسعير، مصمم خصيصاً للهواة بدون خلفية تجارية.

---

## 2. الهوية البصرية والعلامة التجارية

### الألوان الأساسية (Tailwind CSS 4 Theme)
| المتغير | اللون | الاستخدام |
|---------|-------|-----------|
| `gold-main` | `#B8860B` | اللون الرئيسي — أزرار، عناوين، حدود |
| `gold-dark` | `#8B6914` | تدرجات، نصوص بارزة |
| `gold-light` | `#D4A843` | تدرجات فاتحة، هوفر |
| `gold-pale` | `#E8D5A3` | حدود خفيفة، مؤشرات |
| `cream` | `#FDF8E8` | خلفية الصفحات الرئيسية |
| `cream-dark` | `#F5EDD6` | خلفية الفوتر والبطاقات |
| `brown-dark` | `#4A3728` | العناوين الرئيسية |
| `brown-text` | `#5C4033` | النص العادي |

### الخطوط
| المتغير | الخط | الاستخدام |
|---------|------|-----------|
| `font-body` | Noto Naskh Arabic | النص الأساسي |
| `font-reem` | Reem Kufi | اسم العلامة، شعارات |
| `font-amiri` | Amiri | العناوين الفرعية، النص الفاخر |

تُحمّل الخطوط عبر `<link>` في `index.html` مع `preconnect` لتسريع التحميل. يُخفى المحتوى بـ `visibility: hidden` حتى تكتمل تحميل الخطوط (`document.fonts.ready`) أو بعد 1.2 ثانية كحد أقصى.

### الطابع العام
فاخر، تراثي سعودي، عائلي دافئ. التصميم mobile-first بعرض أقصى `max-w-lg` (448px). الصفحة RTL بالكامل (`dir="rtl" lang="ar"`).

### الحركات (Animations)
| الاسم | الاستخدام |
|-------|-----------|
| `fade-in-up` | ظهور الأقسام عند التحميل |
| `float` | حركة خفيفة للشعار |
| `shimmer` | لمعان التدرجات |
| `fade-in` | ظهور سريع |
| `scale-up` | تكبير العناصر التفاعلية |
| `bounce-once` | ارتداد واحد للشارات |

---

## 3. البنية التقنية

### التقنيات المستخدمة (Tech Stack)
| التقنية | الإصدار | الدور |
|---------|---------|-------|
| React | 19.2.0 | واجهة المستخدم |
| Vite | 7.2.4 | أداة البناء وخادم التطوير |
| Tailwind CSS | 4.1.18 | التصميم (utility-first) |
| Firebase | 12.8.0 | الاستضافة + التحليلات + Firestore |
| React Router DOM | 7.12.0 | التنقل بين الصفحات |
| Lucide React | 0.562.0 | الأيقونات |

### تقسيم الحزم (Code Splitting)
يستخدم Vite `manualChunks` لتقسيم الحزمة:
- `firebase-app` — نواة Firebase
- `firebase-analytics` — تحليلات Firebase
- `firebase-firestore` — قاعدة بيانات Firestore
- `react-vendor` — React + React DOM + React Router

الصفحات تُحمّل بشكل كسول (Lazy Loading) عبر `React.lazy()` مع `Suspense` وسبينر ذهبي.

### إعدادات Firebase Hosting
```json
{
  "hosting": {
    "public": "dist",
    "rewrites": [{ "source": "**", "destination": "/index.html" }]
  }
}
```
كل المسارات تُعاد توجيهها إلى `index.html` (SPA routing).

### Google Analytics
- Firebase Analytics (مدمج في التطبيق)
- Google Analytics 4 (gtag) — معرف: `G-8RJ8HHGJJT` (في index.html)
- ~358 مستخدم نشط آخر 28 يوم (اعتباراً من 5 فبراير 2026)

---

## 4. صفحات التطبيق والمكونات

### 4.1 المسارات (Routes)
| المسار | الصفحة | الوصف |
|--------|--------|-------|
| `/` و `/index.html` | `LandingPage` | الصفحة الرئيسية |
| `/menu` و `/menu.html` | `MenuPage` | قائمة المنتجات |
| `/gallery` و `/gallery.html` | `GalleryPage` | معرض صور/فيديو المناسبات |

### 4.2 الصفحة الرئيسية (LandingPage.jsx)
- شعار ثمر النخيل مع خلفية مائية
- زر CTA رئيسي: **"قائمة المنتجات"** (تدرج ذهبي → `/menu`)
- زر CTA ثانوي: **"ضيافة مناسباتكم 🎉"** (تدرج بنفسجي-وردي → `/gallery`)
- قسم "آراؤكم تدفعنا" — كاروسيل صور التقييمات (4 صور، تمرير تلقائي 3 ثوانٍ)
- مكون `EventsShowcase` — معاينة معرض المناسبات
- روابط التواصل: واتساب، إنستقرام، تيك توك
- تتبع: وقت الصفحة، عمق التمرير

### 4.3 قائمة المنتجات (MenuPage.jsx)
- 4 منتجات بالتفاصيل والأسعار
- لكل منتج: صورة، وصف، سعر، شارة مميزة
- **عداد الاختيارات** — شارة تعرض عدد من اختار كل منتج (Firestore حيّ)
- **نظام التقييم** (StarRating) — نجوم بنمط Google مع نصف نجمة
- **نافذة التقييم** (RatingModal) — اختيار نجوم + تعليق اختياري
- **نافذة التعليقات** (CommentsModal) — قراءة فقط، تجلب من Firestore
- زر طلب عبر واتساب لكل منتج
- مكون `EventsShowcase` في أسفل الصفحة
- تتبع: مشاهدة المنتج (Intersection Observer)، وقت الصفحة

### 4.4 معرض المناسبات (GalleryPage.jsx)
- **الهيدر**: عنوان "معرض الصور" + عداد الزوار الحيّ (👁️ X زائر)
- **شبكة 2×N**: 27 عنصر وسائط (7 فيديو + 20 صورة) مصنّفة
- **MediaCard**: بطاقة لكل عنصر:
  - المتصفحات العادية (Safari/Chrome): الفيديو يعمل تلقائياً صامتاً كـ GIF مع شارة "فيديو" في الزاوية
  - المتصفحات الداخلية (Instagram/TikTok): صورة بوستر ثابتة (مُولّدة من الإطار الأول بـ ffmpeg) مع شارة "فيديو"
- **Lightbox**: عرض ملء الشاشة مع تنقل (بدون زوم):
  - الفيديو يعمل تلقائياً بصوت عند الفتح/التنقل (يرجع لصامت إذا المتصفح منع)
  - شريط تقدم (native controls) للتحكم بالتقدم والصوت
  - أيقونة تشغيل ثابتة تظهر عند الإيقاف وتختفي عند التشغيل
  - الصور تعرض بحجمها الطبيعي بدون زوم
- **عداد الزوار**: يبدأ من 347 (مبني على بيانات GA)، يتزايد حيّاً عبر Firestore
- **CTA ثابت**: زر واتساب لاسفل الشاشة "احجز لمناسبتك القادمة 🎉"
- **بوسترات الفيديو**: 7 صور بوستر (event-video-N-poster.jpg) مُولّدة بـ ffmpeg من الإطار الأول لكل فيديو
- حماية من المتصفحات الداخلية (IN_APP_BROWSER) — راجع قسم الحلول

### 4.5 معاينة المناسبات (EventsShowcase.jsx)
- كاروسيل 6 عناصر (3 فيديو + 3 صور) مع تمرير تلقائي كل 4 ثوانٍ
- يظهر في: الصفحة الرئيسية + صفحة المنتجات
- رابط إلى `/gallery`
- نفس حماية IN_APP_BROWSER

---

## 5. نظام Firebase والتحليلات

### 5.1 إعدادات Firebase
| الحقل | القيمة |
|-------|--------|
| Project ID | `thamar-alnakheel` |
| Auth Domain | `thamar-alnakheel.firebaseapp.com` |
| Storage | `thamar-alnakheel.firebasestorage.app` |

### 5.2 Firestore Collections
| المجموعة/المستند | المسار | الاستخدام |
|------------------|--------|-----------|
| عداد الزوار | `counters/galleryVisitors` | `{ count: number }` |
| عداد الاختيارات | `counters/productSelections` | `{ 1: n, 2: n, 3: n, 4: n }` |
| التقييمات | `counters/productRatings` | `{ [id]: { average, count, total } }` |
| عداد التعليقات | `counters/productComments` | `{ [id]: { count } }` |
| التعليقات | `comments/{productId}_{timestamp}` | `{ productId, productName, comment, timestamp }` |

### 5.3 القيم الأولية
| البيان | القيمة | المصدر |
|--------|--------|--------|
| `INITIAL_VISITOR_COUNT` | 347 | بيانات Google Analytics (~358 مستخدم) |
| اختيارات المنتجات | `{1:28, 2:19, 3:15, 4:22}` | بيانات تقديرية |
| التقييمات | `{1:4.8, 2:4.7, 3:4.6, 4:4.8}` | بيانات تقديرية |

**منطق عداد الزوار:**
- يتزايد مرة واحدة لكل جلسة (session) عبر `sessionStorage`
- يستخدم `COUNTER_VERSION = 2` للتحقق من صلاحية بيانات Firestore
- إذا كان الإصدار في Firestore أقدم → يُعاد تعيين العداد إلى `INITIAL_VISITOR_COUNT` تلقائياً
- إذا كان العدد أقل من الحد الأدنى → يُحدّث صامتاً
- يستخدم `onSnapshot` للتحديث الحيّ
- حالة React الابتدائية = `null` (العداد مخفي حتى يرد Firestore — يمنع الوميض)
- لرفع إصدار العداد (إعادة تعيين): زد `COUNTER_VERSION` في `firebase.js`

### 5.4 أحداث التحليلات المتتبعة
| الحدث | الموقع | البيانات |
|-------|--------|----------|
| `page_view` | كل الصفحات | المسار، العنوان |
| `session_start_custom` | أول تحميل | المُحيل، الشاشة، الجهاز |
| `scroll_depth` | كل الصفحات | الصفحة، النسبة (25/50/75/100%) |
| `time_on_page` | كل الصفحات | الصفحة، الثواني |
| `view_product` | المنتجات | اسم المنتج (Intersection Observer) |
| `click_whatsapp_*` | المنتجات/المعرض | المنتج أو الموقع |
| `gallery_visit` | المعرض | الوقت |
| `media_click` | المعرض | النوع، المعرف، الاسم |
| `video_play` | المعرض | المعرف، الاسم |
| `new_gallery_visitor` | المعرض | رقم الزائر |
| `product_selection` | المنتجات | معرف واسم المنتج |
| `submit_rating` | المنتجات | المعرف، التقييم، المتوسط الجديد |
| `submit_comment` | المنتجات | المعرف، الاسم |
| `click_events_showcase` | الكاروسيل | الوسائط الحالية، الفهرس |

---

## 6. المنتجات والتسعير

| المعرف | المنتج | الوزن | السعر | الوصف |
|--------|--------|-------|-------|-------|
| 1 | ذوق المدينة | 1 كيلو | 175 ريال | عجوة المدينة + مكسرات + صوص دبس عضوي |
| 2 | ذوق السكري | 1 كيلو | 150 ريال | سكري مفتّل + بسكويت + صوص طحينة |
| 3 | ذوق نجد | 1 كيلو | 120 ريال | خلاص + أقط + سمن بلدي |
| 4 | المجموعة الكاملة | 1.5 كيلو | 215 ريال | تشكيلة من كل الأذواق |

### رقم الواتساب
`+966596440340`

### حسابات التواصل
- واتساب: `wa.me/966596440340`
- إنستقرام: `@thamar.alnakheel`
- تيك توك: `@thamar.alnakheel`

---

## 7. تطبيق المحاسبة (المشروع الثاني)

### الرؤية
مستشار مالي ذكي للهواة — ليس آلة حاسبة. يُبسّط المصطلحات، يعطي نصائح استباقية مع مرجعيات، ويحسب التكاليف الخفية تلقائياً.

### الفئة المستهدفة
أمهات وهواة يصنعون حلويات التمور في المنزل، بدون خبرة تجارية أو محاسبية.

### المبادئ الأساسية
1. لا تفترض أن المستخدم يفهمك — اشرح "لماذا" وليس فقط "ماذا"
2. كل رقم يحتاج سياق ومرجعية
3. الحسابات في الخلفية، النتائج للمستخدم
4. أقل احتكاك ممكن — قيم افتراضية ذكية
5. التطبيق مستشار يحذر من الأخطاء قبل وقوعها

### هوامش الربح المعيارية
| الهامش | التصنيف |
|--------|---------|
| أقل من 15% | خطر — تخسر أو بالكاد تغطي |
| 15-25% | منخفض — لا يغطي وقتك |
| 25-35% | مقبول — جيد للبدايات |
| 35-45% | ممتاز — موصى به |
| 45%+ | استثنائي — للمنتجات الفاخرة |

**35% = وقتك (15%) + الهالك (5%) + الطوارئ (5%) + الربح الصافي (10%)**

### التكاليف الخفية المحسوبة تلقائياً
| التكلفة | النسبة |
|---------|--------|
| قيمة الوقت | 30 ر.س/ساعة |
| الهالك والتلف | 5% |
| الكهرباء | 3% |
| غير متوقع | 5% |

### فئات المكونات
| الفئة | أمثلة |
|-------|-------|
| التمور 🌴 | عجوة، سكري، خلاص |
| الحشوات 🥜 | مكسرات، بسكويت، أقط |
| التزيين 🍫 | دبس، طحينة، شوكولاتة |
| العلب 📦 | علبة صغيرة، وسط، كبيرة |
| الأكسسوارات 🎀 | نخلة، كرت، أعواد، شريط |

### الصفحات
- `/` — لوحة التحكم (ملخص الأداء + نصائح)
- `/orders` — الطلبات
- `/orders/new` — طلب جديد (3 خطوات)
- `/inventory` — المستودع
- `/calculator` — حاسبة التكلفة
- `/settings` — الإعدادات

### البيانات
تُحفظ في `localStorage` (طلبات، أسعار مخصصة، إعدادات). تصدير/استيراد JSON.

المسار: `thamar-accounting/`
التقنيات: React + Vite + Tailwind + Recharts

---

## 8. تصميم الفلاير A4

تم إعداد prompt لتصميم ورقة A4 عمودية للمعارض (ملف `design-prompt.md`):

- هيدر: شعار ثمر النخيل بخط عربي
- عبارات تسويقية: "آرائكم تدفعنا ⭐" / "نُجهز لكم بكل حب 💛" / "من بيتنا لبيتكم 🏠"
- 3 صور منتجات بإطار ذهبي + أسماء وأسعار
- QR Code كبير (80×80mm+) يوجه إلى الموقع
- فوتر: واتساب + إنستقرام
- المواصفات: 210×297mm، 300 DPI، خلفية كريمي، زخارف عربية خفيفة

---

## 9. المشاكل والحلول (Lessons Learned)

### 9.1 فيديوهات المتصفحات الداخلية (WebView) — المشكلة الأكبر
**المشكلة:** متصفحات إنستقرام وتيك توك الداخلية تعترض أمر `.play()` على عناصر الفيديو وتفتح مشغّل الفيديو الأصلي في شاشة كاملة. مع 7 فيديوهات في شبكة المعرض، كل واحد يستدعي `.play()` → 7 نوافذ منبثقة. إغلاق واحدة يعيد تشغيل React → `.play()` من جديد → حلقة لا نهائية. حتى الانتقال لصفحة المنتجات لا يوقفها لأن EventsShowcase فيها فيديوهات أيضاً.

**الحل:**
```javascript
const IN_APP_BROWSER = (() => {
    if (typeof navigator === 'undefined') return false;
    const ua = navigator.userAgent || '';
    return /Instagram|FBAN|FBAV|TikTok|BytedanceWebview|Line\/|Snapchat|Twitter/i.test(ua);
})();
```
- في المتصفحات الداخلية: لا نستدعي `.play()` أبداً → نعرض صورة بوستر (poster) مُولّدة من الإطار الأول بـ ffmpeg مع شارة "فيديو" في الزاوية
- في المتصفحات العادية: `<video>` حقيقي مع `.play()` برمجياً (GIF-like) + `catch` للفشل
- في Lightbox: autoplay بصوت عند الفتح/التنقل + native controls (شريط تقدم) + أيقونة تشغيل ثابتة عند الإيقاف
- تنظيف: `video.pause()` عند unmount
- **بوسترات الفيديو**: تُولّد بـ `ffmpeg -i video.mp4 -vf "select=eq(n\,0)" -frames:v 1 -q:v 2 poster.jpg` → تحفظ بجانب الفيديو بنفس الاسم مع `-poster.jpg`

**الملفات المتأثرة:** `GalleryPage.jsx` (MediaCard + Lightbox)، `EventsShowcase.jsx`
**ملفات البوستر:** `public/events/event-video-{1-7}-poster.jpg`

### 9.2 خطأ React في خاصية muted
**المشكلة:** React لا يطبّق خاصية `muted` على عنصر `<video>` في DOM فعلياً (خطأ معروف). الفيديوهات تعمل بصوت رغم وجود `muted` في JSX.

**الحل:** استخدام `useRef` + `useEffect` لتعيين `video.muted = true` مباشرة على DOM element.

### 9.3 قفزة عداد الزوار + بيانات Firestore القديمة
**المشكلة (الجولة الأولى):** العداد يعرض العدد القديم من Firestore (مثلاً 24) ثم يقفز للعدد الصحيح — يبدو مشبوه.

**المشكلة (الجولة الثانية):** بعد تغيير `INITIAL_VISITOR_COUNT` من 2000 إلى 347، بقي العدد القديم (1725) في Firestore لأنه أكبر من 347 فلم يُعاد تعيينه. كذلك Firestore cache يعرض قيمة قديمة (24) أولاً ثم القيمة الحية (1725) — وميض مرئي.

**الحل النهائي (نظام الإصدارات):**
- إضافة `COUNTER_VERSION = 2` — رقم إصدار يُحفظ مع العداد في Firestore كحقل `v`
- `subscribeToVisitorCount`: يفحص الإصدار أولاً — إذا أقدم من `COUNTER_VERSION` → يعيد تعيين العداد إلى 347 ويحدّث الإصدار
- `incrementVisitorCount`: يتحقق من الإصدار قبل الزيادة، ويضيف `v` في كل `setDoc`/`updateDoc`
- `useState(null)` بدل `useState(347)` — العداد مخفي تماماً حتى يرد Firestore (يمنع أي وميض)
- العداد يظهر فقط عند `visitorCount !== null`
- **لإعادة تعيين العداد مستقبلاً**: زد `COUNTER_VERSION` في `firebase.js` وأعد البناء والنشر

### 9.4 وميض الخط (FOUT — Flash of Unstyled Text)
**المشكلة:** الخط القديم/الاحتياطي يظهر أولاً، ثم يتحول للخط الصحيح عند اكتمال التحميل.

**الحل:**
- نقل تحميل الخطوط من CSS `@import` (بطيء) إلى `<link>` في `index.html` (أسرع)
- إضافة `<link rel="preconnect">` لـ Google Fonts
- إخفاء الصفحة بـ `visibility: hidden` حتى `document.fonts.ready`
- مهلة أمان 1.2 ثانية كحد أقصى

### 9.5 استعادة الـ Stash
**المشكلة:** ميزة المعرض كاملة كانت في git stash ولم تُضاف للكود. أمر `git checkout stash@{0}` فشل بسبب صلاحيات نظام الملفات.

**الحل:**
- الملفات غير المتتبعة: `git show stash@{0}^3:path > file`
- الملفات المتتبعة: استخراج إلى `/tmp/` ثم نسخ `cp` للمشروع
- البناء نجح بعد الاستعادة

### 9.6 خطأ بناء Vite (EPERM)
**المشكلة:** `npm run build` فشل بـ "EPERM: operation not permitted, unlink dist/.DS_Store"

**الحل:** تمكين صلاحية حذف الملفات على مجلد المشروع.

### 9.7 إزالة الزوم وتبسيط Lightbox
**المشكلة:** ميزة "اضغط على الصورة للتكبير" كانت تسبب تشتت بصري وتعقيد غير ضروري في الواجهة.

**الحل:**
- حذف كل منطق الزوم (`isZoomed` state, `toggleZoom`, `cursor-zoom-in`)
- حذف نص "اضغط على الصورة للتكبير / اضغط للتصغير"
- الصور تُعرض بحجمها الطبيعي (`object-contain`) بدون أي تفاعل إضافي
- إضافة أيقونة تشغيل ثابتة للفيديو تظهر عند الإيقاف (persistent) وتختفي عند التشغيل

### 9.8 عدد زوار غير واقعي
**المشكلة:** تعيين العداد إلى 2000 لا يتوافق مع بيانات GA (358 مستخدم).

**الحل:** اختيار 347 (أقل بقليل من GA) ليبدو طبيعياً وقابلاً للتصديق.

---

## 10. الروابط والمسارات المهمة

### روابط حية
| الرابط | الوصف |
|--------|-------|
| https://thamar-alnakheel.web.app | الموقع المنشور (Firebase Hosting) |
| https://github.com/sa66aam/thamar-landing-page | المستودع على GitHub |

### مسارات الملفات (Mac)
| المسار | الوصف |
|--------|-------|
| `/Users/jo/Pictures/ثمر النخيل` | المجلد الرئيسي |
| `/Users/jo/Pictures/ثمر النخيل/thamar-alnakheel` | موقع التسويق |
| `/Users/jo/Pictures/ثمر النخيل/thamar-accounting` | تطبيق المحاسبة |

### ملفات التوثيق السابقة (الآن مجمّعة هنا)
| الملف | الوصف | الحالة |
|-------|-------|--------|
| `.project-config.json` | إعدادات المشاريع الفرعية | مُدمج هنا |
| `PROJECT_PATH.txt` | المسارات والأوامر السريعة | مُدمج هنا |
| `design-prompt.md` | تصميم فلاير A4 | مُدمج هنا |
| `thamar-alnakheel/README.md` | قالب Vite الافتراضي | لا يحتوي معلومات مهمة |
| `thamar-accounting/PHILOSOPHY.md` | فلسفة تطبيق المحاسبة | مُدمج هنا |
| `thamar-accounting/README.md` | قالب Vite الافتراضي | لا يحتوي معلومات مهمة |

---

## 11. أوامر التشغيل السريعة

### موقع التسويق
```bash
# التطوير
cd "/Users/jo/Pictures/ثمر النخيل/thamar-alnakheel" && npm run dev

# البناء
cd "/Users/jo/Pictures/ثمر النخيل/thamar-alnakheel" && npm run build

# النشر على Firebase
cd "/Users/jo/Pictures/ثمر النخيل/thamar-alnakheel" && firebase deploy

# المعاينة
cd "/Users/jo/Pictures/ثمر النخيل/thamar-alnakheel" && npm run preview
```

### تطبيق المحاسبة
```bash
cd "/Users/jo/Pictures/ثمر النخيل/thamar-accounting" && npm run dev
```

### بناء الكل
```bash
cd "/Users/jo/Pictures/ثمر النخيل/thamar-alnakheel" && npm run build && cd ../thamar-accounting && npm run build
```

---

## 12. خريطة الملفات

### موقع التسويق (`thamar-alnakheel/`)
```
thamar-alnakheel/
├── index.html                    ← نقطة الدخول (خطوط، FOUT fix، GA gtag)
├── firebase.json                 ← إعدادات الاستضافة (SPA rewrite)
├── vite.config.js                ← بناء + code splitting
├── package.json                  ← التبعيات
│
├── src/
│   ├── main.jsx                  ← نقطة الدخول لـ React
│   ├── App.jsx                   ← التوجيه + lazy loading + PageTracker
│   ├── index.css                 ← Tailwind theme + ألوان + خطوط + حركات
│   │
│   ├── lib/
│   │   └── firebase.js           ← Firebase + Analytics + Firestore (كل المنطق)
│   │
│   ├── pages/
│   │   ├── LandingPage.jsx       ← الصفحة الرئيسية
│   │   ├── MenuPage.jsx          ← قائمة المنتجات + تقييمات + تعليقات
│   │   └── GalleryPage.jsx       ← معرض المناسبات + Lightbox + عداد زوار
│   │
│   └── components/
│       ├── EventsShowcase.jsx    ← كاروسيل المناسبات (مشترك)
│       └── BusinessCard.jsx      ← بطاقة العمل
│
├── public/
│   ├── logo.jpg                  ← شعار ثمر النخيل
│   ├── events/                   ← 50+ ملف وسائط (JPG, MP4, MOV)
│   │   ├── event-1.jpg → event-20.jpg
│   │   ├── event-video-1.mp4 → event-video-7.mp4
│   │   └── event-video-1-poster.jpg → event-video-7-poster.jpg  ← بوسترات مُولّدة بـ ffmpeg
│   ├── products/                 ← صور المنتجات
│   ├── reviews/                  ← صور التقييمات
│   └── *.pdf                     ← بطاقات العمل
│
└── dist/                         ← مخرجات البناء (تُنشر على Firebase)
```

### تطبيق المحاسبة (`thamar-accounting/`)
```
thamar-accounting/
├── src/
│   ├── lib/
│   │   ├── advisor.js            ← المستشار المالي
│   │   ├── calculations.js       ← محرك الحسابات
│   │   └── storage.js            ← إدارة البيانات (localStorage)
│   ├── pages/
│   │   ├── HomePage.jsx          ← لوحة التحكم + نصائح
│   │   ├── CalculatorPage.jsx    ← حاسبة التكلفة
│   │   ├── InventoryPage.jsx     ← إدارة المستودع
│   │   └── SettingsPage.jsx      ← الإعدادات
│   ├── components/
│   │   ├── Tooltip.jsx           ← نظام التوجيهات
│   │   └── Layout.jsx            ← الهيكل العام
│   └── data/
│       └── initialData.js        ← البيانات الأساسية والوصفات
└── PHILOSOPHY.md                 ← فلسفة التطبيق (مُدمج أعلاه)
```

---

## 13. ملاحظات النشر والتحديث

### سير العمل (Deployment Flow)
```
تعديل الكود → npm run build → firebase deploy → اختبار على thamar-alnakheel.web.app
```

### ملاحظات مهمة
- **اختبر دائماً في**: Safari + Chrome + متصفح إنستقرام/تيك توك الداخلي
- **عداد الزوار**: إذا احتجت تغيير الحد الأدنى، عدّل `INITIAL_VISITOR_COUNT` في `firebase.js` (العداد في GalleryPage يبدأ `null` ويظهر فقط بعد Firestore)
- **لإعادة تعيين العداد**: زد `COUNTER_VERSION` في `firebase.js` → بناء → نشر
- **الفيديوهات**: لا تضف `autoPlay` مباشرة للفيديو — استخدم ref + `.play()` مع حماية IN_APP_BROWSER
- **بوسترات الفيديو**: عند إضافة فيديو جديد، ولّد البوستر: `ffmpeg -i video.mp4 -vf "select=eq(n\,0)" -frames:v 1 -q:v 2 video-poster.jpg`
- **الخطوط**: لا تستخدم `@import` في CSS — استخدم `<link>` في HTML مع preconnect
- **Git stash**: تحقق دائماً من `git stash list` قبل التطوير — قد توجد ميزات غير مدمجة
- **النشر**: دائماً `npm run build && firebase deploy` (البناء أولاً ثم النشر)

### تاريخ Git المختصر (من الأقدم للأحدث)
1. `Move to Firebase Hosting structure`
2. `Switch to thamar-alnakheel project`
3. `Switch to authentic Naskh fonts (Amiri/Noto)` → تغييرات الخطوط
4. `Update branding, consistent fonts, and add menu page`
5. `Fix WhatsApp number, remove location, add custom messages`
6. `feat: Premium UI overhaul - Glassmorphism, Gold theme`
7. `feat: Complete Analytics tracking system`
8. `chore: Remove GitHub Pages workflow - using Firebase Hosting only`
9. *(Stash)* Gallery feature + Events + Visitor counter + Ratings + Comments
10. `fix: in-app browser video popup loop, visitor counter, lightbox improvements` — بوسترات ffmpeg، حماية WebView، شريط تقدم، عداد إصدارات

---

> **هذا الملف هو المرجع الرئيسي الشامل لمشروع ثمر النخيل.**
> يحل محل جميع الوثائق المتفرقة السابقة.
> يُحدّث مع كل تغيير جوهري في المشروع.
